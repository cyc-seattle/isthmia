#!/usr/bin/env bash
# Opens an interactive SSH session to the substrate VM through IAP.
set -euo pipefail

here="$(dirname "${BASH_SOURCE[0]}")"
read -r name zone < <("$here/substrate-vm")
SHELL=/bin/bash exec gcloud compute ssh "$name" --zone="$zone" --tunnel-through-iap
