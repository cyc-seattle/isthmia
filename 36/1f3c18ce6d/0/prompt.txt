I want to create a new skill in this repository called /work-sesionThe goal is to _quickly_ iterate through small tasks and cleanup. I want the primary agent to be Opus and it will operate in a loop. Basically, I want to describe some small task to be fixed, cleaned up, or just improved for my sanity. The primary agent will clarify any questions and then fire off an implementation sub-agent (Sonnet) to take care of the task. All work will be on one branch to keep things simple. Then, when I'm happy with a "batch" of work, a single PR can be submitted.  The current SDLC described in skills in this project are too heavy-hitting, often spawning multiple PRs for one issue.