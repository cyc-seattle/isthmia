{
  "name": "@cyc-seattle/infrastructure",
  "main": "./src/index.ts",
  "scripts": {
    "prepare": "pnpm run build",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^18.19.130",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@pulumi/docker-build": "^0.0.10",
    "@pulumi/gcp": "^8.41.1",
    "@pulumi/pulumi": "^3.208.0"
  }
}
