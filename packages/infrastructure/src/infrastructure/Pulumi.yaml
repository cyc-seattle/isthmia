name: infrastructure
description: Pulumi-managed GCP infrastructure as code.
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-typescript
