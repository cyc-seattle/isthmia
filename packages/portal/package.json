{
  "name": "@cyc-seattle/portal",
  "description": "Static link portal served at cycsail.team, gated by Google auth (oauth2-proxy)",
  "version": "0.0.1",
  "author": "CYC Community Sailing Center",
  "license": "Apache-2.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "./dist/render.js",
  "types": "./dist/render.d.ts",
  "engines": {
    "node": ">= 20.8 < 23"
  },
  "scripts": {
    "prepare": "pnpm run build",
    "build": "tsc --build && node dist/build.js",
    "clean": "tsc --build --clean"
  }
}
