<task-notification>
<task-id>a992cd54345fd0016</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-ungood-workspaces-personal-isthmia/9366a3a7-c48e-4e24-879a-286617f8edfa/tasks/a992cd54345fd0016.output</output-file>
<status>completed</status>
<summary>Agent "Restructure the justfile" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Report

**Commit:** `e80e647c` — "Restructure the justfile into scripts and recipe groups" on branch `session/deployer-access`.

**Files touched:**
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/justfile`
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/substrate-vm` (new — factors the VM name/zone lookup used by `db-tunnel`, `ssh`, `logs`)
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/db-tunnel` (new — moved recipe body + its four-line comment, condensed to keep the "why," guard preserved)
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/pulumi-tunnel` (new — shared podman/tunnel/wait-loop preamble used by both `deploy` and `preview`, carrying the `#112` and SIGTERM-suppression comments)
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/deploy` (new — `pulumi up --yes`, no args)
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/preview` (new — `pulumi preview`)
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/ssh` (new — IAP SSH to substrate VM)
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/logs` (new — tails a service's `docker compose logs -f` on the VM over IAP SSH)

All seven new scripts are `chmod +x` and pass `bash -n`.

**Verification:**
- `just --list` shows every recipe with exactly one description line, grouped under `[setup]`, `[auth]`, `[dev]`, `[deploy]` (plus ungrouped `default`).
- `just --evaluate` succeeds; `just --dry-run` on `db-tunnel`, `deploy`, `preview`, `ssh`, `logs` shows the expected one/two-line bodies.
- `just fmt`, `just check` (verified it still fails on deliberately unformatted input, then confirmed the file was restored clean), `just test`, and `just ci` all passed.
- I could **not** run `just deploy` or `just preview` to completion — they need live GCP/Pulumi credentials not available here. I verified their scripts only via `bash -n` and `just --dry-run` as instructed.

**Left alone, per scope fence:** `auth-adc`'s `--impersonate-service-account` flag (untouched), `scripts/auth-status` and its recipe (only confirmed it already has a one-line doc comment, made no other change), no `doctor` recipe added, no `deploy-bootstrap` recipe added, `README.md`/`CLAUDE.md`/`.envrc`/`flake.nix`/`packages/**` untouched.

**Noticed but not touched:** `README.md:67` still references `just auth-status`, which the design doc says step 2 (`doctor`) will address — left stale as instructed. Pre-existing ESLint warnings (8x `no-explicit-any` in `clubspot-sdk/src/parse.ts` and `gsuite/src/calendar.ts`) surfaced during `just check`/`just ci`; these are unrelated to this change and were not touched.</result>
<usage><subagent_tokens>60616</subagent_tokens><tool_uses>55</tool_uses><duration_ms>233958</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ab8b34fcb18e0012a</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-ungood-workspaces-personal-isthmia/9366a3a7-c48e-4e24-879a-286617f8edfa/tasks/ab8b34fcb18e0012a.output</output-file>
<status>completed</status>
<summary>Agent "Fix logs compose detection" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Committed.

## Report

**Commit:** `4063c192` — "Detect compose command on the remote host in scripts/logs"

**File touched:** `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/logs` (only file changed, as scoped)

**Change:** Replaced the hardcoded `compose="docker compose ..."` with the same three-way detection COS needs (`docker compose` plugin → `docker-compose` binary → `docker:cli` container fallback), matching the logic in `packages/infrastructure/src/substrate-bootstrap-script.ts:91-94`. The detection code (`detect_dc`) is embedded in the remote command string with `$DC` escaped so it resolves on the remote shell, while `$compose_flags` and `$service` are substituted locally before the string is sent over SSH. Compose flags are unchanged.

**Verification:**
- `bash -n scripts/logs` — passed.
- Simulated the constructed remote command locally (with a stand-in test script in the scratchpad dir, not committed): confirmed the echoed `remote_cmd` contains the full three-way `if/elif/else` detection followed by the exact unchanged flags `--project-directory /var/substrate --env-file /var/substrate/substrate.env -f /var/substrate/docker-compose.yml logs -f &lt;service&gt;`, and that `$DC` correctly resolves inside a nested shell rather than expanding locally.
- `just --evaluate` — succeeded; `just logs service:` recipe signature is untouched (still one `service` argument), and I did not touch `justfile`.
- `just check` — passed (only pre-existing `@typescript-eslint/no-explicit-any` warnings in unrelated files, no errors).
- `just test` — passed (7 files, 72 tests).
- Could not run `just logs` against the real substrate VM — no GCP credentials in this environment, as expected.

**Left alone (not fixed, per scope fence):** Nothing new noticed beyond what the brief already described. No other issues found in `scripts/logs` or `scripts/substrate-vm` during this task.</result>
<usage><subagent_tokens>26620</subagent_tokens><tool_uses>14</tool_uses><duration_ms>75904</duration_ms></usage>
</task-notification>

---

One note on the deployer-access plan before proceeding. Step 3 says to add roles to `user:master@` - do I need to do that if they are the superuser of the account?

---

Wait, can you read https://docs.cloud.google.com/resource-manager/docs/super-admin-best-practices and https://docs.cloud.google.com/resource-manager/docs/creating-managing-folders and recommend how I _should_ consider organizing my accounts (master@ as super admin, ungood@onetrue.name as org admin?)

---

I don't mind having ungood@onetrue.name be org admin given those security risks. I know myself pretty well... 2 questions: is there any _limitation_ to an org admin being external and can ungood@onetrue.name be org admin for multiple orgs (I'm also the admin of my family's google workspace - @onetrue.name)

---

Okay, I want to simplify things. master@ should remain the owner/super-admin. I want ungood@ to be an organizationAdmin. Does that make deployments easier (do I even need this separate deployer role and extra stack?)

---

Okay, I don't think folder-owners are really necessary yet, I'll just have ungoo@ be organizationAdmin and owner of the projects as needed. Continue on #87, but keep scope to what is needed for #118 CI service account.  We'll worry about a second dev when its necessary.

---

Just go, stop waiting for me

---

<task-notification>
<task-id>a08d99e7cab46d83b</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-ungood-workspaces-personal-isthmia/9366a3a7-c48e-4e24-879a-286617f8edfa/tasks/a08d99e7cab46d83b.output</output-file>
<status>completed</status>
<summary>Agent "Add doctor preflight" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Everything checks out. The only remaining references to `auth-status` are historical mentions in the design doc (out of scope to edit) and a provenance comment in `scripts/doctor` itself.

## Report

**Commit:** `1c0fe8a5` — "Add scripts/doctor and just doctor, replacing auth-status"

**Files touched:**
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/doctor` (new, executable)
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/auth-status` (deleted)
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/justfile` (replaced `auth-status` recipe with `doctor`; made `deploy` and `preview` depend on `doctor`)
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/README.md` (rewrote the "Checking your auth state" section)

**Verification:**
- `just check` passed (0 errors; 8 pre-existing `no-explicit-any` warnings in `clubspot-sdk`/`gsuite`, unrelated to this change).
- `just test` passed (7 files, 72 tests).
- `just ci` passed end-to-end (install → build → check → test), exit 0.
- `just --list` shows one line each for `doctor`, `deploy`, `preview` etc. under their groups.
- Manually ran `./scripts/doctor` in the real (healthy) environment: exits 0, prints the three state lines plus ADC identity, then "All checks passed."
- Manually ran it with `PATH=/usr/bin:/bin` (all four tools missing): it still ran gcloud/pulumi/podman-dependent checks defensively, reported all 9 failures (PATH x4, gcloud config, ADC file missing, project describe, pulumi whoami, podman) each with its fix command, and exited 1 — confirming it doesn't stop at the first failure.
- Verified in isolation that the `report_runner_severity` dispatch (`warn`/`fail` as a variable holding a function name) correctly routes to the `warnings` array today, and that promoting it to a failure (step 6) is a genuine one-line change (`report_runner_severity=warn` → `=fail`).
- `grep` confirms no live code/docs reference `auth-status` anymore; only the (out-of-scope) design doc's historical narrative and a one-line provenance comment in `doctor` mention the old name.

**Could not verify live:** the `gcloud projects describe cyc-admin-scripts` and `pulumi whoami` checks were exercised only via their success paths (current environment happens to be authenticated as `master@cyccommunitysailing.org`, which has project access) and via the PATH-missing failure path. I did not have a way to force a *live* 403/expired-credential response from GCP to see that specific failure text, but the code path (`|| fail ...`) is identical in structure to the other verified checks.

**Noticed but left alone (per scope fence):**
- The active gcloud account in this sandbox is currently `master@cyccommunitysailing.org`, not `ungood@onetrue.name` as CLAUDE.md's access policy prescribes for day-to-day work. Not something this task touches — it's pre-existing environment state.
- The credential tables at `README.md:48` and in `CLAUDE.md` still incorrectly claim Pulumi/docker use the `gcloud auth login` session; the design doc assigns that correction to step 5, bundled with the ADC-impersonation flag change, so I left it untouched.
- `just auth-adc` still impersonates `report-runner@`; that's step 5, not this step.</result>
<usage><subagent_tokens>61160</subagent_tokens><tool_uses>40</tool_uses><duration_ms>469987</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a1fea12eeff86112c</task-id>
<tool-use-id>toolu_011dM3WWE7Rsr3DqKWkQF7Do</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-ungood-workspaces-personal-isthmia/9366a3a7-c48e-4e24-879a-286617f8edfa/tasks/a1fea12eeff86112c.output</output-file>
<status>completed</status>
<summary>Agent "Declare jq curl nc in flake" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Report

**Commit:** `9b1b6856` — "Declare jq, curl, and netcat in the devshell for scripts/doctor"

**Files touched:**
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/flake.nix`
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/scripts/doctor`

`flake.lock` was untouched (the rebuild required no lock update).

**Changes:**
- Added `jq`, `curl`, `netcat` to the `devShells.default.packages` list in `flake.nix`, right after `entire`, before the treefmt wrapper.
- Extended the doctor PATH check loop from `gcloud pulumi just podman` to also include `jq curl nc`.

The `netcat` attribute was chosen (not `netcat-gnu` or `openbsd-netcat`) — I verified with `nix build nixpkgs#netcat` that it resolves to a libressl-derived `nc` build and confirmed `bin/nc` exists in the resulting store path before adding it.

**Verification performed:**
- Rebuilt the devshell with `direnv exec . true` — evaluated and built cleanly (pulled `curl`, `libressl`/`nc`, `jq`, and their deps from cache).
- Confirmed all three resolve from the nix store inside the shell:
  - `jq` → `/nix/store/n9ri7m7j3l3ggx7ag8xm87cavjvcxycz-jq-1.8.1-bin/bin/jq`
  - `curl` → `/nix/store/0rrs2ydp7qynml2n29d7v90ra01hxhax-curl-8.20.0-bin/bin/curl`
  - `nc` → `/nix/store/0mg7mgkxc48pyjx80ac42af6ifgiy47l-libressl-4.2.1-nc/bin/nc`
- No TLS/certificate issue was observed with the devshell `curl`, so it was kept in the package list (no fallback to system curl needed).
- `just doctor` exited 0 and printed a resolved ADC identity (`master@cyccommunitysailing.org` in this environment — see note below).
- `just ci` passed: build succeeded, `treefmt --fail-on-change` clean, eslint reported 0 errors / 8 pre-existing `no-explicit-any` warnings in `clubspot-sdk/src/parse.ts` and `gsuite/src/calendar.ts` (unrelated to this change), and vitest reported 72/72 tests passing across 7 files.

**Noticed but left alone (out of scope):**
- `just doctor` in this environment reports the active gcloud account as `master@cyccommunitysailing.org` rather than an individual account impersonating `report-runner@`, which CLAUDE.md's access-policy section says should not be used for day-to-day auth. This is pre-existing environment state, not something introduced by this change, and is outside the scope fence (only `flake.nix` and `scripts/doctor` were to be touched).
- The pre-existing eslint `no-explicit-any` warnings noted above were not touched, per the scope fence.</result>
<usage><subagent_tokens>30183</subagent_tokens><tool_uses>16</tool_uses><duration_ms>86886</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a663917df0e07f1b4</task-id>
<tool-use-id>toolu_01NxHLt52jtVFsu4VXh9eaL1</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-ungood-workspaces-personal-isthmia/9366a3a7-c48e-4e24-879a-286617f8edfa/tasks/a663917df0e07f1b4.output</output-file>
<status>completed</status>
<summary>Agent "Make getClientConfig lazy" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Report

**Commit:** `ec22dce86e2542d60bf46a51d12093872338ea75` — "Make getClientConfig lazy in docker.Image registries"

**Files touched:**
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/packages/infrastructure/src/substrate.ts`
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/packages/infrastructure/src/run-reports-job.ts`

**Changes:**
- Removed the module-scope `const registryToken = gcp.organizations.getClientConfig({}).then(...)` in both files.
- Inlined `gcp.organizations.getClientConfigOutput({}).accessToken` directly at the `registries[0].password` field of each `docker.Image` (`substrate-image` and `report-runner-image`).
- In `run-reports-job.ts`, also inlined the `registryAddress` constant (it was used exactly once) into the `address` field, matching `substrate.ts`'s existing style at that same site — keeping the two structurally parallel, per the design doc's "Auth mirrors run-reports-job.ts" comment.
- Kept the existing "why OAuth2 access token instead of a docker credential helper" comments untouched in both files, and did not touch the `dependsOn: artifactRepositoryAccess` on either `docker.Image`.

**Verification:**
- `grep -rn "getClientConfig" packages/infrastructure/src/` shows only the two `getClientConfigOutput` inline uses — no module-scope calls remain.
- `just check` passed (treefmt clean; eslint shows 8 pre-existing `no-explicit-any` warnings in unrelated files `clubspot-sdk/src/parse.ts` and `gsuite/src/calendar.ts`, 0 errors, nothing in the files I touched).
- `just build` passed, including `tsc --build` for `packages/infrastructure` — confirms `getClientConfigOutput(...).accessToken` (a Pulumi `Output&lt;string&gt;`) typechecks against `password: Input&lt;string&gt;`.
- `just test` passed: 7 test files, 72 tests, all green (no infrastructure-specific test suite exists, per `CLAUDE.md`).
- I did **not** run `pulumi preview` — it needs live GCP credentials and a running podman machine, neither of which I have in this sandbox. The design doc itself notes step 3 is "verified by `pulumi preview` showing no diff"; someone with credentials should confirm that separately. This is a pure refactor (same resource inputs, same `Output`-compatible type), so I have high confidence it produces no diff, but I have not verified it directly.

**Noticed but left alone (out of scope for this step):** everything else in the design doc — the `packages/bootstrap` project, `scripts/doctor`, ADC/ownership changes, service-account migration, and IAM restructuring — is explicitly later steps (4–10) and was not touched.</result>
<usage><subagent_tokens>38701</subagent_tokens><tool_uses>15</tool_uses><duration_ms>57334</duration_ms></usage>
</task-notification>

---

Can you give me a command to do that grant please

---

<task-notification>
<task-id>a0ee6babd2031a692</task-id>
<tool-use-id>toolu_01Vt6XgohwzXdL6KRasqJeSh</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-ungood-workspaces-personal-isthmia/9366a3a7-c48e-4e24-879a-286617f8edfa/tasks/a0ee6babd2031a692.output</output-file>
<status>completed</status>
<summary>Agent "Add bootstrap Pulumi package" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Report

**Commit:** `e0c94a68` — "Add packages/bootstrap with the deployer role and deploy identity"

**Files touched:**
- `packages/bootstrap/package.json`, `tsconfig.json`, `Pulumi.yaml`, `Pulumi.prod.yaml`, `README.md`
- `packages/bootstrap/src/config.ts`, `src/deploy-runner.ts`, `src/index.ts`
- `justfile` (added `deploy-bootstrap` recipe in the `[deploy]` group, depending on `doctor`)
- `docs/manual-setup.md` (updated §2, added new §7 recording the two `master@` grants)
- `pnpm-lock.yaml` (updated by `just install` picking up the new workspace package)

**What `packages/bootstrap` declares:**
- `deploy-runner` service account (`src/deploy-runner.ts`), the CI deploy identity #118 will use.
- The 11 predefined-role `gcp.projects.IAMMember` bindings from the design doc's "Permissions" table, bound to `deploy-runner`.
- The custom `deployer` role (`gcp.projects.IAMCustomRole`) with the exact 7-permission list from the doc, bound via `gcp.projects.IAMMember`.
- `roles/iam.serviceAccountUser` grants to `deploy-runner` on `substrate-runner@…` and `report-runner@…`, addressed by literal email (fully-qualified `projects/&lt;project&gt;/serviceAccounts/&lt;email&gt;` form), since `bootstrap` doesn't own those accounts yet.
- Only additive `gcp.projects.IAMMember`/`gcp.serviceaccount.IAMMember` used; no `IAMPolicy`/`IAMBinding` — verified via `grep -rn "IAMPolicy\|IAMBinding" packages/bootstrap/` (no matches), and a one-line comment in `deploy-runner.ts` explains why (the custom-role permission list itself explains the escalation risk it's guarding against).

**Verification:**
- `just ci` (install → build → check → test) passed, including `tsc --build` of the new package.
- `just check` passed (only pre-existing `no-explicit-any` warnings in unrelated packages, unchanged by this diff).
- `just test` passed (72 tests, 7 files — no new tests needed since this step is pure Pulumi declarations with no testable logic, matching the design doc's "no unit test for a Pulumi program" note).
- `just --list` shows `deploy-bootstrap` under `[deploy]` with one description line.
- Could **not** run `pulumi preview`: no `prod` stack exists yet for the new project (expected — this is the first apply), and creating one (`pulumi stack init`) was blocked by the auto-mode classifier as a state-mutating action. I did not attempt `pulumi up`, per the task's constraint. `tsc --build` succeeding is the compile-time verification available here.

**Deliberately left alone (noticed but out of scope):**
- The predefined-role loop's Pulumi resource name derivation (`role.replace("roles/", "")`) duplicates the same pattern already in `compute.ts`/`run-reports-job.ts`; a shared helper would be nice but isn't part of this step's brief.
- `config.ts` in `packages/infrastructure` (the `deployers` list) is untouched, as required — that's step 10.
- No service accounts, `service-account.ts`, or existing project IAM were moved out of `packages/infrastructure` — that's steps 7–9.
- `docs/manual-setup.md` §2's wording about `config.ts` granting deploy rights is now explained as historical/narrower, but I did not touch `packages/infrastructure/src/config.ts` itself.</result>
<usage><subagent_tokens>85054</subagent_tokens><tool_uses>57</tool_uses><duration_ms>576569</duration_ms></usage>
</task-notification>

---

"ERROR: (gcloud.projects.add-iam-policy-binding) INVALID_ARGUMENT: Request contains an invalid argument.
- '@type': type.googleapis.com/google.cloudresourcemanager.v1.ProjectIamPolicyError
  member: user:ungood@onetrue.name
  role: roles/owner
  type: ORG_MUST_INVITE_EXTERNAL_OWNERS" was the error I got running that command

---

<task-notification>
<task-id>a5cef59d29f9a06f2</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-ungood-workspaces-personal-isthmia/9366a3a7-c48e-4e24-879a-286617f8edfa/tasks/a5cef59d29f9a06f2.output</output-file>
<status>completed</status>
<summary>Agent "Switch ADC to user identity" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Clean working tree, commit landed. Final summary below.

## Report

**Commit:** `25460101eb538a5019e95e1e350d16531d827817` — "Run ADC as your own identity and isolate gcloud config per project"

**Files touched:**
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/.envrc`
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/.gitignore`
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/justfile`
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/README.md`
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/CLAUDE.md`
- `/Users/ungood/workspaces/personal/isthmia/.claude/worktrees/deployer-access/docs/manual-setup.md`

**What changed:**
- `.envrc` now exports `CLOUDSDK_CONFIG="$(git rev-parse --git-common-dir)/../.gcloud"` (with a `mkdir -p`) and `GOOGLE_APPLICATION_CREDENTIALS` pointing at `application_default_credentials.json` inside that same directory, with a comment explaining why both are required (google-auth-library and the Pulumi GCP provider hardcode `$HOME/.config/gcloud` and ignore `CLOUDSDK_CONFIG`).
- `.gitignore` gained `.gcloud/` under the Secrets grouping.
- `justfile`'s `auth-adc` recipe dropped `--impersonate-service-account report-runner@…`; doc comment updated to "Point Application Default Credentials at your own gcloud login."
- `README.md` and `CLAUDE.md` credential tables/prose corrected: `pulumi` and `docker` are documented as authenticating via ADC (not the `gcloud auth login` session), and ADC is now the developer's own identity rather than an impersonation of `report-runner@`. Also corrected the stale claim that deploy rights come from `packages/infrastructure/src/config.ts` — both now point to project `roles/owner` per `docs/manual-setup.md` §7 (this appeared twice in CLAUDE.md — the "Which account" note and the Deployment section's numbered list — both fixed).
- `docs/manual-setup.md` §2's `just auth-adc` bullet updated to drop the impersonation description, since it directly documented the justfile behavior I changed.

**Verification:**
- `just check` — passed (treefmt reformatted the markdown tables I edited on the first run, as expected; second run was clean; eslint reported 8 pre-existing `no-explicit-any` warnings unrelated to this change).
- `just test` — passed (7 test files, 72 tests).
- `grep -rn "impersonate-service-account" justfile README.md CLAUDE.md` — no matches.
- `just doctor` — exits non-zero, as expected: `Active gcloud account: (none)`, `ADC credentials file is missing (.../isthmia/.git/../.gcloud/application_default_credentials.json)`, and `cannot describe the cyc-admin-scripts project`. This is the expected result of pointing `CLOUDSDK_CONFIG`/ADC at a fresh, empty directory. I did not run `just auth-gcp` or `just auth-adc` myself — the developer needs to run both (`just auth-gcp` then `just auth-adc`) to restore a working credential state, which will now populate `/Users/ungood/workspaces/personal/isthmia/.gcloud/` (beside the main checkout) rather than `~/.config/gcloud`.

**Left alone (noticed, not touched):**
- CLAUDE.md's "Access policy" paragraph (just above the corrected table) still says day-to-day development and deploys "run as your own account impersonating a service account — not as an admin." That's now stale too, but it wasn't in the brief's named lines and appears to belong to the larger multi-step design (the `deployers` list/access-policy rewrite is called out as steps 1–4 and step 10 of the same design doc), so I left it for whichever step covers that section.
- Did not touch `scripts/doctor` (explicitly out of scope — that's step 6).
- Did not attempt a deploy or run any interactive `gcloud`/`pulumi` login commands.</result>
<usage><subagent_tokens>72661</subagent_tokens><tool_uses>44</tool_uses><duration_ms>667137</duration_ms></usage>
</task-notification>

---

I did org+project grant for ungood@